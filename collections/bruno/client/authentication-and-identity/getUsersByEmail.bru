meta {
  name: List by email
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/users/by-email
  body: none
  auth: inherit
}

query {
  ~include: 
  ~fields: 
  email: 
}

docs {
  Lists resources with optional filtering, sorting, and pagination.

**Authentication:** Required - Bearer token or API key
**Rate Limit:** 100 requests per minute

}
