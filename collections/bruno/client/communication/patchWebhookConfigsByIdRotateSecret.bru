meta {
  name: Update rotate secret
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/webhook-configs/{id}/rotate-secret
  body: json
  auth: inherit
}

query {
  ~include: 
  ~fields: 
}

body:json {
  {
    "data": {
      "attributes": {},
      "id": "string",
      "relationships": {},
      "type": null
    }
  }
}

docs {
  Updates specific fields of an existing resource.

**Authentication:** Required - Bearer token or API key
**Rate Limit:** 100 requests per minute

}
