meta {
  name: Update change
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/users/auth/password/change
  body: json
  auth: inherit
}

query {
  ~include: 
  ~fields: 
}

body:json {
  {
    "data": {
      "attributes": {
        "current_password": "string",
        "password": "string",
        "password_confirmation": "string"
      },
      "id": "string",
      "relationships": {},
      "type": null
    }
  }
}

docs {
  Change password for authenticated user with current password verification
}
