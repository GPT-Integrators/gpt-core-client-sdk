meta {
  name: Get upload urls
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/extraction/batches/{id}/upload-urls
  body: none
  auth: inherit
}

query {
  ~include: 
  ~fields: 
  ~file_count: 
  ~filenames: 
}

docs {
  Generate presigned URLs for batch document upload
}
