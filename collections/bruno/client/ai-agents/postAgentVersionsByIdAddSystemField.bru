meta {
  name: Create add system field
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/agent-versions/{id}/add-system-field
  body: json
  auth: inherit
}

body:json {
  {
    "data": {
      "system_field_name": "string"
    }
  }
}

docs {
  Add a predefined system field to this version's schema
}
