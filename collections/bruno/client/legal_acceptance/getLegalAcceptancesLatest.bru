meta {
  name: List latest
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/legal-acceptances/latest
  body: none
  auth: inherit
}

query {
  ~filter: 
  ~sort: 
  ~include: 
  ~fields: 
  user_id: 
  document_type: 
}

docs {
  Lists resources with optional filtering, sorting, and pagination.

**Authentication:** Required - Bearer token or API key
**Rate Limit:** 100 requests per minute

}
