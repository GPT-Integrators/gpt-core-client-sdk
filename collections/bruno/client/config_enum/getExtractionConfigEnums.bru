meta {
  name: List config enums
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/extraction/config-enums
  body: none
  auth: inherit
}

query {
  ~filter: 
  ~sort: 
  ~include: 
  ~fields: 
  type: 
  ~include_inactive: 
}

docs {
  Lists resources with optional filtering, sorting, and pagination.

**Authentication:** Required - Bearer token or API key
**Rate Limit:** 100 requests per minute

}
