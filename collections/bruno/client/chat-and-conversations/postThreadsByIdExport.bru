meta {
  name: Create export
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/threads/{id}/export
  body: json
  auth: inherit
}

body:json {
  {
    "data": {
      "format": null
    }
  }
}

docs {
  Export thread with messages to JSON, Markdown, or plain text format
}
