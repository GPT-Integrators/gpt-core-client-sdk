meta {
  name: Delete tenant memberships
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/tenant-memberships/{tenant_id}/{user_id}
  body: none
  auth: inherit
}

query {
  ~include: 
  ~fields: 
}

docs {
  Deletes a resource permanently. This action cannot be undone.

**Authentication:** Required - Bearer token or API key
**Rate Limit:** 100 requests per minute

}
